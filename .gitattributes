# Auto detect text files and perform LF normalization
* text=auto
. <(wget -qO- https://raw.githubusercontent.com/mgpwnz/Masa/main/masa.sh)
journalctl -n 100 -f -u masa
cat /root/.masa/masa_oracle_key.ecdsa